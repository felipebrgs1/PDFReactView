# Server
PORT=3000

# Postgres (compose service uses these defaults)
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_DB=dev
POSTGRES_PORT=5432
# Connection string used by the app and drizzle
DATABASE_URL=postgres://dev:dev@postgres:5432/dev
